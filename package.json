{
  "name": "mrlawassociates",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name=mrlawassociates"
  },
  "devDependencies": {
    "astro": "^5.0.0",
    "wrangler": "^3.90.0"
  }
}
